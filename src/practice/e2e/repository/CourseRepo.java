package practice.e2e.repository;

import java.util.List;

public interface CourseRepo//extends JPARepository<Course,Long>
{
// Fetch courses taken by students with name like 'Sam'
//    @Query(select c FROM Course c JOIN c.enrollments e WHERE e.student.name = LIKE %:name%)
//    List<Course> fetchCoursesByStudentName(@Param("name") String name);

}
