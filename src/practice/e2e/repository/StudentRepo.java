package practice.e2e.repository;

public interface StudentRepo //extends JPARepository<Student,Long>
{
//    // Fetch students enrolled in 'Java Basics'
//    @Query(select s FROM Student s JOIN s.enrollments s WHERE e.course.title = :title)
//    List<Student> findByCourseTitle(@Param("title") String title);
}
