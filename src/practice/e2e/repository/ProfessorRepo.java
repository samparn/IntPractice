package practice.e2e.repository;

import practice.e2e.entity.Professor;

public interface ProfessorRepo //extends JPARepository<Professor,Long>
{
//    @Query( select p from professor p JOIN p.courses c WHERE c.title = :title)
//    List<Professor> findProfessorsByCourseTitle(@Param("title")String title);
}
